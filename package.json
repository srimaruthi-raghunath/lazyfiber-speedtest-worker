{
  "name": "cloudflare-worker-speedtest",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker Speed Test",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
